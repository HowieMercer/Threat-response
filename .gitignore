node_modules/
dist/
.DS_Store
*.log

# The original single-file versions are deliberately NOT ignored.
# They are the only record of what the game looked like before the split,
# and they are small. Keep them committed.

# Screenshots from `npm run verify -- --shots`. Regenerate them; do not
# review a diff of PNGs.
tests/shots/
