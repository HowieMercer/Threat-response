{
  "name": "threat-response",
  "private": true,
  "version": "13.0.0",
  "type": "module",
  "description": "N-able Threat Response — a two-minute ransomware decision simulator",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "node tests/browser.mjs",
    "balance": "node scripts/monte-carlo.mjs",
    "contrast": "node scripts/contrast-audit.mjs",
    "shots": "node scripts/shots.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2",
    "vitest": "^2.1.0",
    "playwright": "^1.48.0"
  }
}
